import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  imports: [],
  template: ` <main></main> `,
  styleUrl: './app.css',
})
export class App {
  // protected readonly title = signal('ql_kho_fe');
}
